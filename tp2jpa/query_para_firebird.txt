-- jpa.fdb
-- 

CREATE TABLE CLIENTE (
    ID INTEGER NOT NULL,
    DESCR VARCHAR(100),
    CONSTRAINT PK_CLIENTE PRIMARY KEY (ID)
);

CREATE TABLE FACTURA (
    NRO INTEGER NOT NULL,
    ID INTEGER NOT NULL,
    IMPORTE DOUBLE PRECISION,
    CONSTRAINT PK_FACTURA PRIMARY KEY (NRO),
    CONSTRAINT FK_FACTURA_CLIENTE FOREIGN KEY (ID)
        REFERENCES CLIENTE(ID)
);


INSERT INTO CLIENTE(ID,DESCR) VALUES (1, 'Juan');

INSERT INTO FACTURA(NRO,ID,IMPORTE) VALUES (100, 1, 500.0);
